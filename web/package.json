{
  "name": "@mobile-ssh/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "xterm.js PWA client: renders the mirrored terminal, sends prompts/keys, surfaces approvals. Served over tailscale-serve HTTPS.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test --test-force-exit \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
